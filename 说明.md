# git 已经提交的文件忽略

# 1. 首先从当前索引中移除
git rm --cached build-profile.json5
git rm --cached oh-package-lock.json5



# 2、使用 git filter-branch 清空历史提交记录
git filter-branch --force --index-filter \
"git rm --cached --ignore-unmatch secret.txt" \
--prune-empty --tag-name-filter cat -- --all